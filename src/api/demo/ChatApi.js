/**
 * Demo chat uses the real v1 Chat API against wave-server demo mode.
 * Conversations are copied from chat_history into the per-runtime demo
 * session on first touch, then user turns append in-session (not to SQLite).
 */
export { ChatApi } from '../v1/ChatApi';
